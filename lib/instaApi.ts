// Instagram API DM gönderim fonksiyonu (taslak)
export async function sendInstagramDM(userId: string, message: string): Promise<boolean> {
  // Burada ileride Instagram API çağrısı olacak
  return true;
}
